# Usage Docs — {{MCP_SERVER_NAME}}

## Tool Reference

### `{{tool_name}}`

**What it does:** {{Full description}}

**When to use:** {{Agent decision guidance}}

**Input:**
```json
{
  "{{param}}": "{{example value}}"
}
```

**Output:**
```json
{
  "{{field}}": "{{example output}}"
}
```

**Example agent usage:**
> "Use the `{{tool_name}}` tool to {{example task}}"

---

## Error Handling

| Error | Cause | Fix |
|---|---|---|
| `{{error_code}}` | {{cause}} | {{fix}} |

## Rate Limits

{{Describe any rate limits or quotas the agent should be aware of.}}
